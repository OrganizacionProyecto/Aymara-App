<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Estado seleccionado (presionado) -->
    <item android:state_selected="true" android:drawable="@drawable/favorito_color" />
    <!-- Estado no seleccionado (normal) -->
    <item android:drawable="@drawable/favorito" />
</selector>
